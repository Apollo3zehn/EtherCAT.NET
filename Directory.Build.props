<Project>

  <Import Project="./build/build.props" />

  <PropertyGroup>
    <PackageLicenseUrl Condition="$(PackageLicenseUrl) == ''">https://github.com/Apollo3zehn/EtherCAT.NET/blob/master/LICENSE.md</PackageLicenseUrl>
    <PackageProjectUrl Condition="$(PackageProjectUrl) == ''">https://github.com/Apollo3zehn/EtherCAT.NET</PackageProjectUrl>
  </PropertyGroup>

</Project>